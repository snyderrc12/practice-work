// Explanation of Variables 

var favColor = 'red';

// we assign this in our code so we don't have to know that favColor is red. JS knows

// you can put double quotes or single quotes for a string or number

var myArray = ['blue', 'red', 'green'];
var numOfFavColors = 3; 

// basically a list; numbers, variables, functions
// with the array you don't have to say favColor 2 is blue, favColor 3 is red...
// strings need quotes but numbers don't

var hasGotFavColors = true;

// boolean - true or false 

var richObject = {
	firstName: 'Rich',
	lastName: 'Armstrong',
	favColors: ['blue', 'red', 'green'],
	yearsAlive: 103,
	isMale: true
};

// Object - like an array, but you can give it a name for every list item. 

// richObject.isMale
// true

